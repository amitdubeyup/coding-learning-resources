## Accessibility & Internationalization
- What are accessibility (a11y) best practices in frontend development?
- What are ARIA roles and how do you use them?
- How do you test and ensure accessibility in your applications?
- What is internationalization (i18n) and localization (l10n)?
- How do you implement i18n in a React or frontend app?
- What are CSS-in-JS solutions? (e.g., Styled Components, Emotion)
- How do you handle animations in frontend (CSS transitions, Framer Motion, GSAP)?
- What are the pros and cons of using CSS modules vs. global CSS?
- How do you handle browser compatibility and polyfills?
- What are web components and custom elements?
- How do micro-frontends communicate with each other?
- How do you manage state normalization and entity management?
- How do you use Lighthouse and Core Web Vitals for performance optimization?
- What are common frontend security concerns (XSS, CSRF, CSP) and how do you mitigate them?
- How do you use Service Workers and caching strategies for performance and offline support?
- What is static site generation (e.g., Next.js, Gatsby) and when would you use it?
- What are the basics of configuring Webpack, Rollup, or Vite?
- How do you use and write tests with Jest, React Testing Library, or Cypress?
- How do you handle frontend logging and analytics?

# Frontend Interview Questions

## React.js
- What is React.js?
- What is the difference between Virtual DOM, Shallow DOM, and the regular DOM in React?
- What are controlled and uncontrolled components?
- What are hooks in React?
- Explain `useEffect`, `useState`, `useMemo`, and `useCallback` hooks in detail.
- What is JSX? What are Babel and Webpack?
- What is Redux? Explain reducer, action, and store in Redux.
- What is middleware in Redux?
- Explain data flow in Redux.
- What is Redux-Thunk? What is Redux-Saga? Difference between Redux-Thunk and Redux-Saga.
- What is the difference between class components and function components?
- How can we implement `componentWillUnmount` in a function component?
- Explain lifecycle methods in React.
- What is the difference between `export default` and `export` in React?
- What is a portal in React?
- What is reconciliation in React?
- What is `useRef` in React?
- What is server-side rendering (SSR) in React?
- What is `useStrict` in React?
- What is a fragment in React?
- What is React Router?
- What is a node module in React?
- What is the default localhost server port in React.js? How can we change it?
- What is a higher-order component (HOC) in React?
- What is a pure component in React?
- What is the difference between state and props?
- How do you optimize a React app?
- What is the difference between React.js and Angular.js?
- What is prop drilling and how do you overcome it?
- What is the Context API in React?
- What are `super`, `constructor`, and `render` functions in React?

## React.js (Additional)
- How is React.js different from other libraries/frameworks?
- What is the difference between JSX and TSX in React.js?
- What is the Shadow DOM in React.js?
- How is the virtual DOM different from the real DOM?
- What are the different types of components in React.js?
- What is the state in React.js?
- What are props and the difference between props and state?
- How does re-rendering happen when state changes in React.js?
- What happens if you try to update the state directly in React.js?
- How do you share data among sibling components?
- What are styled components? Have you implemented theming in React.js?
- What is a React server component?
- How do functional components handle the lifecycle methods of class components?
- How do you handle side effects in class and functional components?
- List out hooks you have used in React so far.
- What are the rules of hooks in React?
- What are custom hooks? Provide an example.
- What is the difference between useState and useRef in form handling?
- What are the differences between useState, useContext, and useReducer?
- What is the difference between useCallback and useMemo?
- What is the difference between Context API and Redux?
- What is Redux Toolkit? How does it simplify Redux?
- How would you implement a global state without Redux?
- What is a synthetic event in React?
- What is the difference between a synthetic event and a real DOM event?
- How do you pass arguments to event handlers in React?
- How do you handle dynamic routing in React?
- How do you manage form validation in React? Give examples.
- How would you implement a multistep form with conditional rendering?
- How do you optimize forms with hundreds of input fields in React?
- What are keys in React lists and why are they important?
- What is React.memo and how does it work?
- What is virtualization in React?
- How do you optimize rendering of large lists in React?
- How do you avoid unnecessary re-rendering in React?
- What is the purpose of error boundaries in React?
- How do you handle errors in async operations in React?
- How do you fetch data in React? Give examples.
- What is Axios and how is it different from fetch API?
- How do you securely store API keys in React?
- How would you implement retry logic for failed API requests in React?
- How do you identify and fix memory leaks in React?
- How do you build React applications for production?
- What is the purpose of the .env file in React?

## HTML
- What is `<!DOCTYPE html>` in HTML5?
- What is the difference between `<div>` and `<span>`?
- What are semantic and non-semantic tags?
- What is the difference between HTML and HTML5?
- What is the `<iframe>` tag in HTML5?
- What are formatting tags in HTML?
- What is the difference between `<b>` and `<strong>`?
- What is the viewport attribute in HTML?
- What is an attribute in HTML?
- What are block-level and inline elements?

## HTML
- Difference between CSS BEM methodology and normal CSS?

## JavaScript & TypeScript
- What is ECMAScript?
- What is the difference between `let`, `const`, and `var`?
- What are the spread operator, rest operator, and default parameters?
- What is deep copy and shallow copy?
- What are promises, callback functions, and async/await?
- What is the difference between promises and callbacks?
- What is event bubbling and event capturing?
- What is a higher-order function?
- Explain different types of functions in JavaScript.
- What is an arrow function?
- Why do we use `call`, `apply`, and `bind` methods?
- How many ways can you create an object in JavaScript?
- What is prototype inheritance?
- What is TypeScript?
- What are common array and string methods?
- What is the difference between Java and JavaScript?
- What are throttling and debouncing?
- What is `null` and `undefined`?
- What are falsy values?
- What is execution context, event loop, stack, call queue, and microtask queue?
- What are `setTimeout` and `setInterval`?
- What are `Object.seal` and `Object.freeze`?
## React.js
- What are `WeakMap` and `WeakSet`?

- What are `sessionStorage`, `localStorage`, and cookies?

- Write a program to sort an array.

- What is the use of `JSON.stringify` and `JSON.parse()`?

- What are `map`, `filter`, and `reduce` in JavaScript?
